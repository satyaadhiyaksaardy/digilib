<p style='visibility: hidden; position: fixed;width: 0px;height: 0px;'>
    {{ asset('storage/covers') }}/{{ $book->cover_path }}</p><img
    src="{{ asset('storage/covers') }}/{{ $book->cover_path }}" alt="{{ $book->title }}" width="120px">
